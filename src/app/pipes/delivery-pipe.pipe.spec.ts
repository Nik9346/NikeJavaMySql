import { DeliveryPipePipe } from './delivery-pipe.pipe';

describe('DeliveryPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DeliveryPipePipe();
    expect(pipe).toBeTruthy();
  });
});
